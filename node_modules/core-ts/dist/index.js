'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');

class SimpleEntity {
    constructor(id = 0, name = '') {
        this.id = id;
        this.name = name;
    }
    equals(another) {
        return this.id == another.id &&
            this.name.toUpperCase() == another.name.toUpperCase();
    }
    renderForSelect() {
        return React.createElement("option", { key: this.id, value: this.id }, this.name);
    }
    renderForDataList() {
        return React.createElement("option", { key: this.id, value: this.name }, this.id);
    }
    renderForPicking() {
        return React.createElement("tr", null, this.name);
    }
    static fromJSON(json) {
        return new SimpleEntity(json.id, json.name);
    }
}

exports.SimpleEntity = SimpleEntity;
